@*
  Componente auxiliar: redireciona para /login quando o usuário não está autenticado.
  Não usa returnUrl — o Login.razor sempre redireciona para "/" após autenticação.
*@
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/login");
    }
}
